#include <stdio.h>

int main() {
    int n, count = 0;
    scanf("%d", &n);

    for (int k = 2; k <= n; k++) {
        if (n % k == 0) {
            count++;
        }
    }

    printf("%d\n", count);
    return 0;
}
